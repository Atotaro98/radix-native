# Quote
> Inline quotation. Wraps text in curly quotes.

```tsx
import { Quote } from 'radix-native'
<Text>She said <Quote>hello world</Quote></Text>
```
Props: `m, mx, my, mt, mr, mb, ml` (MarginToken).
